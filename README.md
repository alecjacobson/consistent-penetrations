Header only implementation of "Consistent Penetration Depth Estimation"
[Heidelberg 2007] using libigl and embree.

Build example with:

    mkdir build
    cd build 
    cmake ../
    make

Test example with:

    ./example

Include in your project with

    #include "consistent_penetrations.h"
